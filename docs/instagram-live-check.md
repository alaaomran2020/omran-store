# Instagram feed live check

تم نشر commit `a8212b1` بنجاح، ونجح workflow `Deploy Next.js site to Pages` رقم `32494771901` في بناء ونشر الموقع.

عند فحص `https://omrantoys.store` بعد النشر مباشرة، ظهر الموقع العام بحالته القديمة ولم يظهر نص قسم Instagram بعد. البحث عن «آخر جديد من عمران للألعاب» لم يجد النص. هذا يشير إلى تأخر انتشار/تخزين مؤقت على النطاق المخصص أو أن GitHub Pages ما زال يخدم نسخة سابقة وقت الفحص؛ لا يدل على فشل البناء، لأن خطوة Next.js build وخطوة deploy نجحتا.

يجب إعادة الفحص بعد انتهاء التخزين المؤقت أو فتح الموقع مع query string جديد، ثم تشغيل Sync Instagram feed بعد إعداد الأسرار.
